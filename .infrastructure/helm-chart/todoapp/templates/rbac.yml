kind: ServiceAccount
apiVersion: v1
metadata:
  name: {{ .Values.todoapp.rbac.ServiceAccount }}
  namespace: {{ .Values.common.namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: {{ .Values.common.namespace }}
  name: {{ .Values.todoapp.rbac.ServiceAccount }}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "watch", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.todoapp.rbac.ServiceAccount }}-binding
  namespace: {{ .Values.common.namespace }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.todoapp.rbac.ServiceAccount }}
roleRef:
  kind: Role
  name: {{ .Values.todoapp.rbac.ServiceAccount }}
  apiGroup: rbac.authorization.k8s.io
